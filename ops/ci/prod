# The purpose of this script is to be exactly what is executed by the CI
# (Buildkite) 'command' for production deployment.

git lfs pull
# The argument to "rake deploy:local_prod" is the number of blocks to use.
nix develop --command rake deploy:local_prod 436391
