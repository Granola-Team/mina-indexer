# The purpose of this script is to be exactly what is executed by the CI
# (Buildkite) 'command' for tier-3 testing.

ulimit -Sn
ulimit -Hn
nix develop --command just tier3 422056

# The argument to `just tier3` is the block height to retrieve up to.
# Be sure to remove `blocks.list` if it exists so that it refetches all blocks.
