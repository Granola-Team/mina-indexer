steps:

- label: "Build"
  command: |
    echo HOME=$HOME
    echo PATH=$PATH
    echo TEMP=$TMPDIR
    nix-shell --pure --run just
    nix-shell --pure --run "just prereqs"
    nix-shell --pure --run "just test-ci"
  key: prereqs
  agents:
    os: linux
